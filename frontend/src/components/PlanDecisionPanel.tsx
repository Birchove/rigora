export function PlanDecisionPanel() {
  return (
    <section className="inline-panel" aria-label="方案确认说明">
      <strong>等待确认</strong>
      <p>接受、修改或坚持等选项只使用服务器给出的 command 与 candidate ID。</p>
    </section>
  );
}
